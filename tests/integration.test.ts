import { describe,it,expect } from 'vitest';
import * as T from 'three';
import { Player } from '../src/player/Player';
import { World } from '../src/world/World';
import { Enemies } from '../src/enemies/Enemies';
import { ThirdPerson } from '../src/camera/ThirdPerson';
import { C } from '../src/config/gameplay';
import type { Command } from '../src/input/Input';
const scene=new T.Scene(),world=new World(scene);
const openWorld={blocked:()=>false,move:(position:T.Vector3,x:number,z:number)=>position.add(new T.Vector3(x,0,z)),solids:[]} as unknown as World;
const command:Command={x:0,z:1,run:false,crouch:false,jump:false,fire:false,reload:false};
describe('integração da simulação sem renderizador',()=>{
  it('anda, corre, agacha e normaliza diagonais',()=>{const move=(c:Command)=>{const p=new Player();p.position.set(0,0,0);for(let i=0;i<60;i++)p.update(1/60,c,0,world);return p.position.length();};const walk=move(command),run=move({...command,run:true}),crouch=move({...command,crouch:true});expect(walk).toBeGreaterThan(4.5);expect(run).toBeGreaterThan(7);expect(crouch).toBeLessThan(2.6);expect(move({...command,x:1})).toBeCloseTo(walk,3);});
  it('pula e pousa; sem salto duplo',()=>{const p=new Player();p.update(1/60,{...command,z:0,jump:true},0,world);expect(p.position.y).toBeGreaterThan(0);for(let i=0;i<20;i++)p.update(1/60,{...command,z:0},0,world);const velocity=p.vertical;p.update(1/60,{...command,z:0,jump:true},0,world);expect(p.vertical).toBeLessThan(velocity);for(let i=0;i<90;i++)p.update(1/60,{...command,z:0},0,world);expect(p.position.y).toBe(0);});
  it('não atravessa parede nem sai da área',()=>{const pos=new T.Vector3(-12,0,-8);for(let i=0;i<200;i++)world.move(pos,0,-.08,C.player.radius);expect(pos.z).toBeGreaterThan(-10.1);const edge=new T.Vector3(0,0,42);world.move(edge,0,3,C.player.radius);expect(edge.length()).toBeLessThanOrEqual(C.world.radius-C.player.radius+.001);});
  it('câmera respeita limite vertical e aproxima antes da cobertura',()=>{const cam=new ThirdPerson();cam.look(0,1e5,1);expect(cam.pitch).toBe(C.camera.maxPitch);cam.look(0,-1e5,1);expect(cam.pitch).toBe(C.camera.minPitch);cam.pitch=0;cam.update(1,new T.Vector3(-12,0,-19),world);expect(cam.camera.position.z).toBeLessThan(-15.5);expect(cam.camera.position.y).toBeGreaterThan(.3);});
  it('inimigo persegue, ataca, morre e é removido',()=>{const p=new Player(),enemies=new Enemies(scene,world);p.position.set(0,0,0);expect(enemies.spawn(1,p.position)).toBe(true);const e=enemies.active[0];e.avatar.root.position.set(0,0,-4);enemies.update(1/60,p,()=>{});expect(e.state).toBe('CHASE');expect(e.avatar.root.position.z).toBeGreaterThan(-4);e.avatar.root.position.set(0,0,-1);e.cooldown=0;enemies.update(1/60,p,()=>{});expect(e.state).toBe('ATTACK');expect(p.health.value).toBe(86);expect(enemies.damage(e,C.weapon.damage)).toBe(false);expect(enemies.damage(e,C.weapon.damage)).toBe(true);expect(e.state).toBe('DEAD');expect(enemies.active).toHaveLength(0);expect(e.avatar.root.parent).toBeNull();enemies.clear();});
  it('inimigo não causa dano através de uma cerca',()=>{const p=new Player(),enemies=new Enemies(scene,world);p.position.set(11,0,-12.5);enemies.spawn(1,p.position);const e=enemies.active[0];e.avatar.root.position.set(11,0,-13.5);e.cooldown=0;enemies.update(1/60,p,()=>{});expect(p.health.value).toBe(100);enemies.clear();});
  it('inimigos mantêm separação suave enquanto atacam',()=>{const p=new Player(),enemies=new Enemies(scene,world);p.position.set(0,0,0);enemies.spawn(1,p.position);enemies.spawn(1,p.position);const [a,b]=enemies.active;a.avatar.root.position.set(-.05,0,-1);b.avatar.root.position.set(.05,0,-1);a.speed=b.speed=2;const before=a.avatar.root.position.distanceTo(b.avatar.root.position);for(let n=0;n<60;n++)enemies.update(1/60,p,()=>{});expect(a.avatar.root.position.distanceTo(b.avatar.root.position)).toBeGreaterThan(before+.5);expect(a.state).toBe('ATTACK');expect(b.state).toBe('ATTACK');enemies.clear();});
  it('nasce longe vagando sem conhecer automaticamente o jogador',()=>{const p=new Player(),enemies=new Enemies(scene,openWorld);p.position.set(0,0,0);enemies.spawn(1,p.position);const e=enemies.active[0];e.avatar.root.position.set(0,0,C.enemy.detection+4);enemies.update(C.enemy.perceptionInterval,p,()=>{});expect(e.state).toBe('WANDER');expect(e.target).toBeUndefined();enemies.clear();});
  it('vaga, muda de direção e pode fazer uma pausa curta',()=>{const p=new Player(),enemies=new Enemies(scene,openWorld);enemies.spawn(1,p.position);const e=enemies.active[0];e.avatar.root.position.set(0,0,30);e.wanderDirection.set(1,0,0);e.wanderTimer=2;e.pauseTimer=0;const before=e.avatar.root.position.clone();enemies.update(1,p,()=>{});expect(e.avatar.root.position.x).toBeGreaterThan(before.x);e.wanderTimer=0;enemies.update(.01,p,()=>{});expect(e.pauseTimer).toBeGreaterThan(0);expect(e.state).toBe('WANDER');enemies.clear();});
  it('adquire dentro da percepção e desiste além da distância maior',()=>{const p=new Player(),enemies=new Enemies(scene,openWorld);p.position.set(0,0,0);enemies.spawn(1,p.position);const e=enemies.active[0];e.avatar.root.position.set(0,0,C.enemy.detection-1);enemies.update(C.enemy.perceptionInterval,p,()=>{});expect(e.state).toBe('CHASE');expect(e.target).toBe(p);p.position.set(0,0,C.enemy.disengage+e.avatar.root.position.z+1);enemies.update(C.enemy.perceptionInterval+.02,p,()=>{});expect(e.state).toBe('WANDER');expect(e.target).toBeUndefined();enemies.clear();});
  it('escolhe o jogador vivo mais próximo e ignora o morto',()=>{const near=new Player(),far=new Player(),enemies=new Enemies(scene,openWorld);near.position.set(2,0,0);far.position.set(7,0,0);enemies.spawn(1,near.position);const e=enemies.active[0];e.avatar.root.position.set(0,0,0);enemies.update(C.enemy.perceptionInterval,[far,near],()=>{});expect(e.target).toBe(near);near.health.damage(C.player.health);enemies.update(1/60,[far,near],()=>{});expect(e.target).toBe(far);enemies.clear();});
  it('mantém muitos Borrões distantes espalhados em WANDER',()=>{const p=new Player(),enemies=new Enemies(scene,openWorld);for(let n=0;n<18;n++){enemies.spawn(5,p.position);const angle=n/18*Math.PI*2;enemies.active[n].avatar.root.position.set(Math.sin(angle)*24,0,Math.cos(angle)*24);}for(let n=0;n<30;n++)enemies.update(1/60,p,()=>{});expect(enemies.active).toHaveLength(18);expect(enemies.active.every(enemy=>enemy.state==='WANDER'&&!enemy.target)).toBe(true);const spread=Math.max(...enemies.active.map(a=>Math.max(...enemies.active.map(b=>a.avatar.root.position.distanceTo(b.avatar.root.position)))));expect(spread).toBeGreaterThan(40);enemies.clear();});
  it('Caçador persegue de qualquer distância, troca para o vivo mais próximo e ignora morto',()=>{const dead=new Player(),alive=new Player(),enemies=new Enemies(scene,openWorld);dead.position.set(30,0,0);alive.position.set(-25,0,0);expect(enemies.spawnHunter(10,alive.position)).toBe(true);const hunter=enemies.active[0];hunter.avatar.root.position.set(0,0,0);enemies.update(C.enemy.perceptionInterval+.1,[dead,alive],()=>{});expect(hunter).toMatchObject({kind:'hunter',state:'CHASE',target:alive});dead.position.set(2,0,0);enemies.update(C.enemy.perceptionInterval+.1,[dead,alive],()=>{});expect(hunter.target).toBe(dead);dead.health.damage(C.player.health);enemies.update(1/60,[dead,alive],()=>{});expect(hunter.target).toBe(alive);enemies.clear();});
});
